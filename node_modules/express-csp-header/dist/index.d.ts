import { CSPHeaderParams } from 'csp-header';
import { RequestHandler, Request, Response } from 'express';
import { ParseOptions } from 'parse-domain';
export * from './constants';
declare type ReportUriFunction = (req: Request, res: Response) => string;
export interface ExpressCSPParams extends Omit<CSPHeaderParams, 'reportUri'> {
    domainOptions?: ParseOptions;
    reportOnly?: boolean;
    reportUri?: string | ReportUriFunction;
}
export declare function expressCspHeader(params?: ExpressCSPParams): RequestHandler;
